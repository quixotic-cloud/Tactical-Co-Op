class X2Ability_ACV extends X2Ability;

var array<X2DataTemplate> Empty;

static function array<X2DataTemplate> CreateTemplates()
{
	// Restore the Ram ability from P4 if this unit should return
	return default.Empty;
}
